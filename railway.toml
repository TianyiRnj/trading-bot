[build]
builder = "nixpacks"

[deploy]
# Single worker is mandatory: multiple workers would start multiple bot threads.
startCommand = "gunicorn --workers 1 --threads 4 --bind 0.0.0.0:${PORT:-5000} app:app"
healthcheckPath = "/healthz"
healthcheckTimeout = 300
restartPolicyType = "on-failure"
